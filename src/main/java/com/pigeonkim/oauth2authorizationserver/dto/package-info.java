/**
 * Request/response payloads for the web layer, carrying Bean Validation constraints.
 * Kept separate from {@code domain} entities so persistence shape never leaks over HTTP.
 */
package com.pigeonkim.oauth2authorizationserver.dto;
